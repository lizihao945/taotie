<% if current_user.already_likes?(@like.food) %>
  $( "#food_like_<%= @like.food.id %>" ).html( "已喜欢 (<%= @like.count %>)" );
<% else %>
  $( "#food_like_<%= @like.food.id %>" ).html( "喜欢 (<%= @like.count %>)" );
<% end %>