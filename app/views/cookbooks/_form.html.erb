<%= form_for(@cookbook) do |f| %>
  <% if @cookbook.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cookbook.errors.count, "error") %> prohibited this cookbook from being saved:</h2>

      <ul>
      <% @cookbook.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.label :difficulty_level %><br />
    <%= f.number_field :difficulty_level %>
  </div>
  <div class="field">
    <%= f.label :time_level %><br />
    <%= f.number_field :time_level %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :tips %><br />
    <%= f.text_field :tips %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
