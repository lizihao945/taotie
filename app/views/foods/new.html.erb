<div class='container'>
	<div class='page-header'>
		<h1>New food</h1>
	</div>
	<div class='row'>
		<div class='col-md-3'>
			<%= render 'form' %>
		</div>
		<div class="category_tags col-md-5">
			<h3>美食标签</h3>
			<% FoodCategory.all.each do |category| %>
			<button type="button" class="btn btn-default">
				<%= category.name %></button>
			<% end %></div>
		</div>
</div>